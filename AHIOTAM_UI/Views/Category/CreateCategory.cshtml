@model CreateCategoryDto

@{
    ViewData["Title"] = "Kategori Ekle";
    Layout = "~/Views/Admin/Index.cshtml";
}

<div class="flex flex-wrap -mx-3">
    <div class="flex-none w-full max-w-full px-3">
        <div class="relative flex flex-col min-w-0 mb-6 break-words bg-white border-0 shadow-xl dark:bg-slate-850 dark:shadow-dark-xl rounded-2xl bg-clip-border">
            <div class="p-6 pb-0 mb-0 border-b-0 border-b-solid rounded-t-2xl border-b-transparent">
                <h6 class="dark:text-white text-lg font-semibold">Kategori Ekle</h6>
            </div>
            <div class="flex-auto p-6">
                <form asp-action="CreateCategory" asp-controller="Category" method="post" class="space-y-4">
                    <div>
                        <label asp-for="CategoryName" class="block mb-2 text-sm font-medium text-slate-700 dark:text-white">Kategori Adı</label>
                        <input asp-for="CategoryName" class="w-full px-3 py-2 border rounded-lg shadow-sm dark:bg-slate-800 dark:text-white" placeholder="Kategori adını giriniz" />
                        <span asp-validation-for="CategoryName" class="text-red-500 text-sm"></span>
                    </div>
                    <div class="flex justify-end space-x-2 mt-6">
                        <a href="/Category/Index" class="px-4 py-2 text-sm font-semibold text-slate-600 dark:text-white border rounded-lg">İptal</a>
                        <button type="submit" class="px-4 py-2 text-sm font-semibold text-white bg-gradient-to-r from-emerald-500 to-teal-400 rounded-lg shadow-md hover:opacity-90">Kaydet</button>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>


